/// @description textbox_text_selected_set(textbox, selectedStart, cursorPosition)
/// @function textbox_text_selected_set
/// @param textbox
/// @param  selectedStart
/// @param  cursorPosition
with(argument0)
{
    selectedStart = argument1;
    cursor = argument2;
}
